image: kitware/vtk-for-ci:v9.1.0
stages:
  - build
  - test

build-module:
  stage: build
  script:
    - mkdir -p build
    - cd build
    - cmake -DVTK_DIR=/vtk/install/lib/cmake/vtk-9.1/ -DBUILD_TESTING=ON -DCMAKE_BUILD_TYPE=Release ..
    - cmake --build . --parallel 2
  tags:
    - docker
    - linux
    - vtk
  artifacts:
    expire_in: 1h
    when: always
    paths:
      - build/

test-module:
  stage: test
  script:
    - Xvfb :99 -screen 0 1280x1024x24 &
    - cd build
    - export DISPLAY=:99
    - ctest -j 2 --output-on-failure || ctest -j 1 --rerun-failed -VV
  tags:
    - docker
    - linux
    - vtk
  dependencies:
    - build-module
  needs:
    - build-module
